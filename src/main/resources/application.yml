# application.yml
server:
  port: 8080 # 默认端口

spring:
  profiles:
    active: dev # 【关键】默认激活 dev 环境（本地开发）

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # 邮件配置 (假设生产和开发共用，如果不同请移到 application-dev/prod.yml)
  mail:
    host: smtp.qq.com
    port: 587
    username: 754351669@qq.com
    password: smpdrbtzsffebegj
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
        debug: true

# MyBatis 配置
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

# 阿里云 OSS
aliyun:
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    keyid: LTAI5tFFTQxcwrNM7TpmsTkk
    keysecret: ZuGO8yo71CRZh5xJ6SjtpyaBe3anrg
    bucketname: subtlechat01

# JWT 配置
chatroom:
  jwt:
    admin-secret-key: fsuit_admin_secret
    admin-ttl: 604800000
    admin-token-name: Authorization
    user-secret-key: fsuit_user_secret
    user-ttl: 604800000
    user-token-name: Authorization
