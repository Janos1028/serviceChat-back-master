# application-prod.yml
server:
  port: 9090 # ??????

spring:
  # ????? (?????????????)
  datasource:
    url: jdbc:mysql://localhost:3306/service_chat?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver

    username: root1
    password: 123456

  # ?? Redis
  redis:
    host: localhost
    port: 6379
    database: 1

# ????????
chatroom:
  cors:
    allowed-origins:  http://120.55.5.60:90
